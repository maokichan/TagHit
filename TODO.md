# TagHit 待办（TODO）

> **唯一待办清单**。进度只记 `docs/CONTEXT.md` §三；裁决纪律见 CONTEXT §四；parked（不预建模）项同时登记于 DECISIONS 末尾。
> 组织方式：主线 → 远期/架构级 → 前端·按组件 → 前后端边界/契约 → 工程化。
> 条目格式：内容（依赖/裁决状态，记录日期）。

## 主线（下一步）

1. **标准功能组件与详情界面翻新**（2026-09-12 指定）：来源根树已落地；剩余——浏览视图（item 网格/卡片）、tag 面板、条目详情界面，以及各功能组件的**专属全页内容页**（角标目前全页复用窄面板，过渡形态）。
2. **三方插件发现/分发**：机制（声明/装载/隔离/权限面）全部就绪；待裁决目录扫描、安装、启停管理的分发形态。

## 远期 / 架构级

- **三方插件权限面执行**：manifest.surfaces v0 只声明不校验；分发落地时补执行层（权限门接在 registerContributedFeature）。
- **EAV 元数据建模**：词表已留位（元数据 = 核心事实 + EAV 长尾），领域/存储未建。
- **标签语义带出**：标签关联（tag→tag 有向）已建模，读取端投影未消费关联。
- **事件系统**（D6）：扫描进度 → 进度 UI；裁决基线"只保留有真实订阅者的事件"。
- **config 持久化迁宿主**：现为 localStorage；迁移后存储键形状不变（`featureId:key`）。
- **后台增量扫描 / C-S / 作品嵌套·多归属**：DECISIONS parked 项，动前先立裁决。

## 前端 · 按组件

- **壳 / 键鼠**：
  - 节点右键可见性操作（2026-09-12 裁决：**键鼠交互框架就位后开发**）——节点行右键菜单：隐藏该节点全部子条目 / 仅显示该节点或其子条目；依赖 MenuContext 扩展 node 目标类型。语义基线：可见性 = 当前工作区浏览/排序/搜索的成员前提（browseWorkspace 投影已如此），全局搜索跨工作区不 gate。
  - 多选进阶：Shift 区间选择、拖拽多选；顺带修四个边界点——Ctrl 双击误开详情（dblclick 未滤修饰键）、Esc/点空白不清多选集、缩略图未设 `draggable=false`（做条目拖拽前必须处理）。
- **ItemGrid / ItemCard（浏览视图）**：随主线 1 翻新。
- **TagsPanel / 标签**：随主线 1 翻新；**tag 修饰**（parked：语义与形态均未讨论，动前先裁决）。
- **ItemDetail（条目详情）**：随主线 1 翻新。
- **FilesPanel（文件管理）**：新建文件/目录操作；移动目标仍手输 → 复用 `dialog.pickDirectory`。
- **StartScreen**：内联搜索收敛到 globalSearch 功能组件（消除双实现）。
- **媒体**：封面提取升级（缩略图落库已就绪，可自动截取首帧之外的封面）。

## 前后端边界 / 契约

- **条目拖出应用**（2026-09-12 记录）：拖出得到 `taghit-file://` 协议地址，应交付真实文件路径（系统间拖放语义）。
- **TEXT_EXTS 双表收敛**：文本预览白名单若频繁变动，契约加拉取端点（现为双表硬编码）。
- **thumbnail.save 宿主闸**：2MiB 上限在宿主端点内实现，boundary 校准（s34）覆盖不到宿主层——若扩宿主层测试需单列。

## 工程化

- **LICENSE**：未定未加。
- **CI**：四份校准 + 五层 typecheck 可直接脚本化。
- **测试三件套落地**（2026-09-12 方案裁决：成熟工具为主，自研只留场景）：Vitest（运行器/断言，承接校准脚本可选）、fast-check（property-based，打输入边界）、Playwright（Electron E2E；把 GUI 冒烟脚本化：建工作区→挂根→树操作→右键）。
